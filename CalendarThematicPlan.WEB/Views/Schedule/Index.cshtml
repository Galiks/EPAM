
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>

    <div>
        @{Html.RenderPartial("PartialGetSchedules");}
    </div>

    <div>
        @if (User.IsInRole("Admin"))
        {
            using (Html.BeginForm("Create", "Schedule"))
            {
                <button class="list-inline">Создать расписание</button>
            }
        }
    </div>
</body>
</html>
